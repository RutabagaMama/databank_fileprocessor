<%= form_with(model: message_in, local: true) do |form| %>
  <% if message_in.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_in.errors.count, "error") %> prohibited this message_in from being saved:</h2>

      <ul>
      <% message_in.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
