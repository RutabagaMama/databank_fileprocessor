<%= form_with(model: message_out, local: true) do |form| %>
  <% if message_out.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message_out.errors.count, "error") %> prohibited this message_out from being saved:</h2>

      <ul>
      <% message_out.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
